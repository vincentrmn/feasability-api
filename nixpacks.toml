[phases.setup]
# python311 (fournit aussi libstdc++ pour shapely) + wkhtmltopdf (binaire sur le
# PATH, utilise pour le rendu PDF cote serveur : pas de find_library, compatible
# Nixpacks) + fontconfig/police pour le texte du PDF.
nixPkgs = ["python311", "wkhtmltopdf", "fontconfig", "dejavu_fonts"]
