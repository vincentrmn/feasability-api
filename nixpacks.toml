[phases.setup]
# python311 (libstdc++ pour shapely) + wkhtmltopdf (assemblage PDF texte+images) +
# librsvg (rsvg-convert : rasterise les schemas SVG en PNG, le wkhtmltopdf de Nix
# rendant mal le SVG inline) + fontconfig/police pour le texte.
nixPkgs = ["python311", "wkhtmltopdf", "librsvg", "fontconfig", "dejavu_fonts"]
